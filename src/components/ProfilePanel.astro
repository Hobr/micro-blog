---
import type { ProfileData } from "../data/profile";

interface Props {
    profile: ProfileData;
}

const { profile } = Astro.props;
---

<section>
    <h2>{profile.name}</h2>
    <p>{profile.role}</p>
    <p>{profile.bio}</p>
</section>
