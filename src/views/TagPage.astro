---
import PostList from "../components/PostList.astro";
import type { AlternateLink, Locale } from "../i18n/config";
import type { SiteDictionary } from "../i18n/dictionary";
import Layout from "../layouts/Layout.astro";
import type { BlogPost } from "../lib/posts";

interface Props {
    locale: Locale;
    dictionary: SiteDictionary;
    tag: string;
    posts: BlogPost[];
    alternateLinks: AlternateLink[];
}

const { locale, dictionary, tag, posts, alternateLinks } = Astro.props;
---

<Layout
    title={dictionary.tagPage.metaTitle(tag)}
    locale={locale}
    dictionary={dictionary}
    alternateLinks={alternateLinks}
>
    <main class="page-shell" id="main-content">
        <section class="terminal-panel tag-results-panel">
            <div class="tag-results-panel__heading">
                <p class="terminal-prompt">
                    <span aria-hidden="true">$</span>
                    {dictionary.tagPage.panelSubtitle(tag)}
                </p>
                <h1>#{tag}</h1>
            </div>
            <PostList
                posts={posts}
                locale={locale}
                emptyMessage={dictionary.tagPage.empty}
            />
        </section>
    </main>
</Layout>
